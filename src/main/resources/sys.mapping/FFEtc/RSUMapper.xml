<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="FFEtc.mapper.RSUMapper">
    <select id="selectTransStatusRes_ByTime" resultType="FFEtc.entity.RSUTransStatusRes">
        SELECT TransStatus AS TransStatus,COUNT(*) as TransStatusNum from transdata WHERE DetectTime &gt;= #{dtfrom} and DetectTime &lt; #{dtto} group by TransStatus
    </select>
</mapper>